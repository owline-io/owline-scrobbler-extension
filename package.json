{
  "name": "owline-scrobbler-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Owline Scrobbler Chrome extension",
  "scripts": {
    "test": "node --test tests/*.test.js tests/providers/*.test.js",
    "lint": "eslint . --max-warnings 0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
